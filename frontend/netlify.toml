[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# The _redirects file in the public folder will handle routing
# No need for duplicate redirects here
